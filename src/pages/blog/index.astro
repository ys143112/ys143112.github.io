---
import Layout from '../../layouts/Layout.astro';
import Archive from '../../components/Archive.astro';
import { getPosts } from '../../lib/posts';
const posts = await getPosts();
---
<Layout title="기록"><div class="page-heading shell"><span class="eyebrow blue">FIELD NOTES</span><h1>배움을 남기는 곳.</h1><p>작업의 과정, 해결한 문제, 새롭게 생긴 질문을 기록합니다.</p></div><Archive posts={posts}/></Layout>
